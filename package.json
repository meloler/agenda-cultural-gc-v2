{
  "name": "agenda-cultural-gc-v2",
  "version": "0.1.0",
  "private": true,
  "description": "Agenda Cultural GC controlled rebuild workspace.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "install:workspaces": "npm install",
    "dev": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/web run build && npm --workspace packages/event-intelligence run typecheck",
    "lint": "npm --workspace apps/web run lint && npm --workspace packages/event-intelligence run lint",
    "typecheck": "npm --workspace apps/web run typecheck && npm --workspace packages/event-intelligence run typecheck",
    "test": "npm --workspace packages/event-intelligence run test && npm --workspace apps/web run test",
    "validate": "npm run typecheck && npm run lint && npm run build && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meloler/agenda-cultural-gc-v2.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/meloler/agenda-cultural-gc-v2/issues"
  },
  "homepage": "https://github.com/meloler/agenda-cultural-gc-v2#readme",
  "dependencies": {
    "@supabase/supabase-js": "^2.97.0",
    "dotenv": "^17.3.1"
  }
}
